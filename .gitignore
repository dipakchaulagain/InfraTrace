# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
.venv/
venv/
*.egg-info/

# Environment / secrets — NEVER commit these. Single .env at the repo root
# (see .env.example) for both Docker Compose and local dev.
.env
*.env

# Node
node_modules/
frontend/dist/
frontend/.vite/

# Build artefacts
dist/
build/
*.tsbuildinfo
frontend/vite.config.js
frontend/vite.config.d.ts

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Postgres data
postgres_data/

# DB backup files — contain full production data, never commit these.
# The ./backup directory itself is created by docker-compose's bind mount
# and intentionally kept out of git; see README's Backup & Restore section.
backup/

# Excluded from version control per request
Prototype script/
docs/

# Local Claude Code tool settings (machine-specific, not app config)
.claude/

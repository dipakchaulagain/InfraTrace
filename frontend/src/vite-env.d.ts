/// <reference types="vite/client" />

// Injected at build time via vite.config.ts's `define`, sourced from
// package.json's version field — kept in sync with the git release tag.
declare const __APP_VERSION__: string
